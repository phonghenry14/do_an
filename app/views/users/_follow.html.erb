<%= link_to "Follow", follow_relationship_path(@user), class: 'follow btn btn-primary', method: :put, remote: true, data: { toggle_text: 'Unfollow', toggle_href: unfollow_relationship_path(@user), id: @user.id } %>
