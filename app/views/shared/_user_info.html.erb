<h1>
  <%= gravatar_for current_user %>
  <%= current_user.name %>
</h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.statuses.count, "status") %></span>
